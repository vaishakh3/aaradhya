# Publish the Google Sheet tab as CSV and paste its export URL here.
# Expected columns: id,name,category,subtitle,fabric,description,colors,image,image_alt,instagram_url,active,sort
GOOGLE_SHEETS_CSV_URL=

# Product images can be Cloudinary delivery URLs pasted into the sheet's image column.
